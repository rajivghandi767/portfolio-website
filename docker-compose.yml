services:
  # -----------------------------------------
  # Database Layer
  # -----------------------------------------
  portfolio-local-db:
    image: postgres:18-alpine
    container_name: portfolio-local-db
    restart: unless-stopped
    env_file: .env.example
    networks:
      - portfolio-local
    ports:
      - "5432:5432"
    volumes:
      - portfolio-local-db-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U portfolio-user -d portfolio-db"]
      interval: 5s
      timeout: 5s
      retries: 5

  # -----------------------------------------
  # Caching Layer
  # -----------------------------------------
  portfolio-local-redis:
    image: redis:7-alpine
    container_name: portfolio-local-redis
    restart: unless-stopped
    networks:
      - portfolio-local
    ports:
      - "6379:6379"

  # -----------------------------------------
  # Django Backend (Smart Server)
  # -----------------------------------------
  portfolio-local-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: portfolio-local-backend
    env_file: .env.example
    networks:
      - portfolio-local
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    depends_on:
      portfolio-local-db:
        condition: service_healthy
      portfolio-local-redis:
        condition: service_started
    command: >
      sh -c "python manage.py migrate && 
             python manage.py seed_data && 
             python manage.py runserver 0.0.0.0:8000"

  # -----------------------------------------
  # React Frontend (Dumb Client)
  # -----------------------------------------
  portfolio-local-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: portfolio-local-frontend
    env_file: .env.example
    networks:
      - portfolio-local
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - portfolio-local-backend

volumes:
  portfolio-local-db-data:
    name: portfolio_local_db_data

networks:
  portfolio-local:
