# -----------------------------------------------------------------------------
# DJANGO CORE SETTINGS
# -----------------------------------------------------------------------------
# A secret key for a particular Django installation. This is used to provide cryptographic signing.
# Generate a new one for production. You can use an online generator.

DJANGO_SECRET_KEY=your-super-secret-key-here

# Set to 'production' for production environment or 'local' for development

DJANGO_ENV=local

# -----------------------------------------------------------------------------
# DJANGO SECURITY SETTINGS
# -----------------------------------------------------------------------------
# A comma-separated list of strings representing the host/domain names that this Django site can serve.
# For production, this should be your backend domain/container name(s).

# Example: portfolio-api.rajivwallace.com, portfolio-api

ALLOWED_HOSTS=localhost,127.0.0.1,portfolio-api

# A comma-separated list of origins that are trusted for cross-site requests.
# For production, this should be your frontend's domain(s).

# Example: https://rajivwallace.com,https://www.rajivwallace.com

CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173

# For local development, a list of allowed origins for CORS. In production, the nginx container handles CORS and headers.

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173

# -----------------------------------------------------------------------------
# POSTGRESQL DATABASE SETTINGS
# -----------------------------------------------------------------------------

POSTGRES_DB=portfolio-db                            # The name of the database.
POSTGRES_USER=portfolio-user                        # The PostgreSQL user to connect with.
POSTGRES_PASSWORD=your-secure-postgres-password     # The password for the PostgreSQL user.
POSTGRES_HOST=portfolio-db-host                     # The hostname where the PostgreSQL server is running.
POSTGRES_PORT=5432                                  # The port the PostgreSQL server is listening on.

# -----------------------------------------------------------------------------
# FRONTEND (VITE/REACT) SETTINGS
# -----------------------------------------------------------------------------
# The base URL for the backend API that the frontend will connect to.
# This should be the public URL of your Django backend in production.

# Example: https://portfolio-api.rajivwallace.com

VITE_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# NOTIFICATION & EMAIL SETTINGS
# -----------------------------------------------------------------------------
# The webhook URL for sending notifications to a Discord channel.

DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your/webhook

# -----------------------------------------------------------------------------
# OPTIONAL PRODUCTION SETTINGS
# -----------------------------------------------------------------------------
# The URL path for accessing the Django admin interface.
# Example: my-secret-admin-path/

ADMIN_URL=admin/